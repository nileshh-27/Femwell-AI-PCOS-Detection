[build]
  command = "npm run build"
  publish = "dist/public"

# Proxy API calls from Netlify -> Render backend.
[[redirects]]
  from = "/api/*"
  to = "https://femwell-ai-pcos-detection.onrender.com/api/:splat"
  status = 200
  force = true
